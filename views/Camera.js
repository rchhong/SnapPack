import React, {Component} from 'react'
import {
    View,
    Text,
    TouchableOpacity,
} from 'react-native'

export default class Camera extends Component {
    constructor(props)
    {
        super(props);
    }

    render()
    {
        return (

        );
    }
}